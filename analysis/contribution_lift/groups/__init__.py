"""Per-group contribution-lift replay specs (RC v3 T5).

One module per T5 issue group; each exposes a module-level ``SPEC:
ReplaySpec`` and is appended to ``analysis.contribution_lift.registry.SPECS``.
"""
