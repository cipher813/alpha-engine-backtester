[pytest]
testpaths = tests
markers =
    parity: opt-in replay parity test (Phase 1.1) — requires trades.db + ArcticDB access. Excluded from default collection; run via `pytest -m parity`.
addopts = -m "not parity"
